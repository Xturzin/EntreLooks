// Em desenvolvimento: copie para config.js e ajuste a URL local
// Em produção: gerado automaticamente pelo build.sh via variável API_URL do Vercel
const CONFIG = {
   API_URL: "http://127.0.0.1:8000"
}